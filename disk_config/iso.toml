[customizations.installer.kickstart]
contents = """
timezone Europe/Vienna --utc
keyboard --xlayouts=us,de --switch=grp:alt_shift_toggle

%post
bootc switch --mutate-in-place --transport registry ghcr.io/srvb/srvb-os:latest

font_path="$(find /usr/share/fonts/FiraCode -type f -name 'FiraCodeNerdFont-Regular.ttf' -print -quit)"
test -n "${font_path}"
install -d /boot/grub2/fonts /etc/default
grub2-mkfont -s 24 -o /boot/grub2/fonts/FiraCodeNerdFont.pf2 "${font_path}"
touch /etc/default/grub
sed -i '/^GRUB_GFXMODE=/d' /etc/default/grub
echo 'GRUB_GFXMODE=auto' >> /etc/default/grub
sed -i '/^GRUB_FONT=/d' /etc/default/grub
echo 'GRUB_FONT=/boot/grub2/fonts/FiraCodeNerdFont.pf2' >> /etc/default/grub
grub2-mkconfig -o /boot/grub2/grub.cfg

flatpak install --system --noninteractive flathub \
  io.github.kolunmi.Bazaar \
  app.zen_browser.zen \
  org.gnome.baobab \
  org.gnome.Calculator \
  org.gnome.gitlab.somas.Apostrophe \
  org.gnome.Loupe \
  org.gnome.Papers \
  app.drey.KeyRack \
  org.gnome.TextEditor

# Anaconda writes a "/" line into /etc/fstab, but composefs (used by bootc)
# rejects the remount systemd-remount-fs.service tries to apply from it at
# every boot (mount: /: fsconfig ... overlay: No changes allowed in
# reconfigure). Drop just that line, keep /boot, /boot/efi, swap, etc.
# See: https://github.com/bootc-dev/bootc/issues/971
awk '$2 != "/"' /etc/fstab > /etc/fstab.new && mv /etc/fstab.new /etc/fstab
%end
"""

[customizations.installer.modules]
enable = [
  "org.fedoraproject.Anaconda.Modules.Storage",
  "org.fedoraproject.Anaconda.Modules.Runtime",
  "org.fedoraproject.Anaconda.Modules.Network",
  "org.fedoraproject.Anaconda.Modules.Localization",
  "org.fedoraproject.Anaconda.Modules.Security",
  "org.fedoraproject.Anaconda.Modules.Services",
  "org.fedoraproject.Anaconda.Modules.Users",
  "org.fedoraproject.Anaconda.Modules.Timezone"
]

disable = [
  "org.fedoraproject.Anaconda.Modules.Subscription",
]
